# Copy to .env.local and fill in. All are optional (sensible fallbacks exist).

# Booking link used by every primary CTA.
NEXT_PUBLIC_CALENDLY_URL="https://calendly.com/your-org/ops-strategy-call"

# Optional: a real walkthrough video for the "See it in action" block.
# Drop the file in /public (e.g. /public/walkthrough.mp4) or use a hosted URL.
# NEXT_PUBLIC_WALKTHROUGH_URL="/walkthrough.mp4"
