node_modules
.next
out
build
.DS_Store
*.pem
npm-debug.log*
.env
.env.*
!.env.example
.vercel
*.tsbuildinfo
next-env.d.ts

# Internal source material — this repo is PUBLIC, and everything listed here
# is still sitting in the project root. Entries for the case-study uploads
# (the metrics and client PDFs, UPLABS Asset, Royal Key Supply, PRIMAS BCC) and
# the loose issue.png / response.jpeg were dropped once those files left the
# working tree; re-add them here BEFORE putting that kind of material back.
brand material/
leakproof-project-brief.html
Leakproof-Page-Prompt.md

# local editor/agent config
.claude/
